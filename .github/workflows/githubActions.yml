name: CI/CD Pipeline

on:
  push:
    branches: ['main', 'develop']
  pull_request:
    branches: ['*']

defaults:
  run:
    shell: bash

jobs:
  essentials:
    name: 🛠️ essentials
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./template/check_draft
